name: SubscriptionTracker
options:
  bundleIdPrefix: com.yourdomain
targets:
  SubscriptionTracker:
    type: application
    platform: iOS
    deploymentTarget: "15.0"
    sources:
      - path: .
        excludes:
          - project.yml
          - .github
          - .git
          - DerivedData
          - Payload
          - SubscriptionTracker.xcodeproj
          - SubscriptionTracker.xcworkspace
    dependencies:
      - package: Firebase
        product: FirebaseAuth
      - package: Firebase
        product: FirebaseFirestore
      - package: GoogleSignIn
        product: GoogleSignIn
    settings:
      GENERATE_INFOPLIST_FILE: YES
      CURRENT_PROJECT_VERSION: 1
      MARKETING_VERSION: 1.0
      # These settings are essential for standard SwiftUI apps
      INFOPLIST_KEY_UIApplicationSceneManifest_Generation: YES
      INFOPLIST_KEY_UILaunchScreen_Generation: YES
      PRODUCT_BUNDLE_IDENTIFIER: com.yourdomain.SubscriptionTracker
      SWIFT_VERSION: 5.0
      CODE_SIGN_STYLE: Automatic
      CODE_SIGN_IDENTITY: ""
      # DEVELOPMENT_TEAM: "YOUR_TEAM_ID" # You must replace this with your Apple Developer Team ID
      INFOPLIST_KEY_CFBundleURLTypes:
        - CFBundleURLSchemes:
            - "com.googleusercontent.apps.1034308202455-cf35fd24g97ncmmv6b3kdr7emcdq2n74"
packages:
  Firebase:
    url: https://github.com/firebase/firebase-ios-sdk.git
    from: 11.2.0
  GoogleSignIn:
    url: https://github.com/google/GoogleSignIn-iOS.git
    from: 7.1.0
